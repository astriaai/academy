/**
 * Artboard 2 — initial /artboard prompt.
 *
 * Starts a fresh tutorial chat, but the brief is intentionally explicit
 * about reference ownership: each character keeps the garment assigned in
 * the workspace templates, and garments must not migrate between people.
 */
import baseScript from "../artboard/02a-artboard-command.js";

export default baseScript;
